<UserControl x:Class="dot_net_fm.FileGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:dot_net_fm"
             Focusable="True">

    <UserControl.Resources>
        <SolidColorBrush x:Key="HoverHighlightBrush" Color="#1FFFFFFF"/>

        <Style x:Key="ScrollThumbs" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="RectangleCorner" 
                                Background="#55FFFFFF" 
                                CornerRadius="4" 
                                Margin="2,0,2,0" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RectangleCorner" Property="Background" Value="#88FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="RectangleCorner" Property="Background" Value="#AAFFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Bg" Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True" IsEnabled="{TemplateBinding IsEnabled}">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False" />
                                </Track.DecreaseRepeatButton>
                                
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollThumbs}" />
                                </Track.Thumb>
                                
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollViewer">
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundBrush}" ClipToBounds="True"
          PreviewMouseLeftButtonDown="FileView_PreviewMouseLeftButtonDown"
          PreviewMouseMove="FileView_PreviewMouseMove"
          PreviewMouseLeftButtonUp="FileView_PreviewMouseLeftButtonUp"
          PreviewMouseRightButtonUp="FileView_PreviewMouseRightButtonUp">

        <ScrollViewer x:Name="FileScrollViewer"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Background="Transparent"
                      PreviewMouseWheel="FileScrollViewer_PreviewMouseWheel">
            
            <ItemsControl x:Name="FolderItemsControl" Margin="24"
                          ItemsSource="{Binding Folders, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True" Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="local:FolderItem">
                        <Border x:Name="ItemBorder" Cursor="Hand" 
                                Margin="4" CornerRadius="4"
                                MouseLeftButtonDown="ItemBorder_MouseLeftButtonDown">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource HoverHighlightBrush}"/>
                                        </Trigger>
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="Background" Value="#33FFFFFF"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="4"
                                        Width="{Binding IconSize, RelativeSource={RelativeSource AncestorType=local:FileGridView}, ConverterParameter=2.2}">
                                
                                <Image x:Name="IconImage" Stretch="Uniform" Margin="0,0,0,6"
                                       Width="{Binding IconSize, RelativeSource={RelativeSource AncestorType=local:FileGridView}}"
                                       Height="{Binding IconSize, RelativeSource={RelativeSource AncestorType=local:FileGridView}}"
                                       Source="{Binding NativeIcon}"/>

                                <TextBlock x:Name="NameText" Text="{Binding Name}" FontSize="11" 
                                           Foreground="{StaticResource TextPrimaryBrush}" 
                                           HorizontalAlignment="Center" TextAlignment="Center" LineHeight="14"
                                           TextWrapping="Wrap"
                                           MouseLeftButtonDown="NameText_MouseLeftButtonDown">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}"
                                         Background="#333333" Foreground="{StaticResource TextPrimaryBrush}"
                                         BorderBrush="{StaticResource IconBrush}" BorderThickness="1"
                                         FontSize="11" Padding="2,1"
                                         HorizontalAlignment="Stretch" VerticalContentAlignment="Center"
                                         KeyDown="RenameBox_KeyDown" LostFocus="RenameBox_LostFocus">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                                <TextBlock Text="{Binding ItemCount}" FontSize="10" 
                                           Foreground="{StaticResource TextSecondaryBrush}" 
                                           TextWrapping="Wrap"
                                           HorizontalAlignment="Center" TextAlignment="Center" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <Canvas x:Name="SelectionCanvas" IsHitTestVisible="False">
            <Border x:Name="SelectionBorder"
                    Background="#220078D7" BorderBrush="#CC0078D7" BorderThickness="1"
                    Visibility="Collapsed"/>
        </Canvas>
    </Grid>
</UserControl>
